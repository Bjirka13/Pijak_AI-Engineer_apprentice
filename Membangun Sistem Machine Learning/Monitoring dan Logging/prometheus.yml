global:
  scrape_interval: 15s
  evaluation_interval: 15s

rule_files:
  - monitoring/alert_rules.yml

scrape_configs:
  - job_name: shipment-delay-inference
    metrics_path: /metrics
    static_configs:
      - targets:
          - localhost:8000
